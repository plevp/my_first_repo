#!/usr/bin/python

import sys

from collections import *

Person = nametuple('Person', "ID MARKS NAME CLASS");


def doit():

    n = int(raw_input().strip())
    titles = raw_input().strip().split();
    l = []
    for _ in xrange(n):
        s = raw_input().strip().split();
        l.append(

if __name__ == "__main__":
    doit()
